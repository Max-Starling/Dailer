import React from 'react';
import PropTypes from 'prop-types';

import './RepeatableSettings.scss';

const RepeatableSettings = (props) => {
  return (
    <div styleName="settings">
      123
    </div>
  );
}

export default RepeatableSettings;
